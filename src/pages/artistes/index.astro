---
import Layout from '../../layouts/Layout.astro';
import PocketBase from 'pocketbase';

const pb = new PocketBase('http://127.0.0.1:8090');

const allArtistes = await pb.collection('artistes').getFullList({
  sort: 'nom'
});

const allProgrammes = await pb.collection('programme').getFullList({
  expand: 'artiste,scene',
  sort: 'date'
});

const passagesByArtiste = allProgrammes.reduce((acc: any, p: any) => {
  const artisteId = p.artiste;
  if (!acc[artisteId]) {
    acc[artisteId] = [];
  }
  acc[artisteId].push(p);
  return acc;
}, {});

const artistesAvecPassages = await Promise.all(
  allArtistes.map(async (artiste: any) => {
    const passages = passagesByArtiste[artiste.id] || [];
    return { ...artiste, passages };
  })
);

function toDate(value: string | number | Date): Date {
  return new Date(value);
}

function formatDatePassage(dateValue: string | number | Date): string {
  const d = toDate(dateValue);
  const day = new Intl.DateTimeFormat('fr-FR', { day: 'numeric', month: 'long' }).format(d);
  const time = new Intl.DateTimeFormat('fr-FR', { hour: '2-digit', minute: '2-digit' }).format(d);
  return `${day}, ${time}`;
}

function getDayKey(dateValue: string | number | Date): string {
  const d = toDate(dateValue);
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

function getTodayKey(): string {
  const d = new Date();
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

function getTomorrowKey(): string {
  const d = new Date();
  d.setDate(d.getDate() + 1);
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

const searchQuery = Astro.url.searchParams.get('search') ?? '';

let filteredArtistes = artistesAvecPassages.filter((artiste: any) => {
  const matchSearch = !searchQuery || artiste.nom.toLowerCase().includes(searchQuery.toLowerCase());
  
  return matchSearch;
});

filteredArtistes = filteredArtistes.filter((artiste) => artiste.passages.length > 0);

function getImageUrl(artiste: any): string | null {
  const imageField = artiste.photo_artiste;
  if (imageField) {
    return `http://127.0.0.1:8090/api/files/artistes/${artiste.id}/${imageField}`;
  }
  return null;
}

function buildUrl(search: string, date: string): string {
  const params = new URLSearchParams();
  if (search) params.set('search', search);
  if (date && date !== 'tous') params.set('date', date);
  const query = params.toString();
  return query ? `/artistes?${query}` : '/artistes';
}
---

<Layout titre="Artistes">
  <main class="min-h-screen bg-[#a9c8f7] pb-24">
    <section class="bg-linear-to-b from-[#a9c8f7] to-[#8eb3f0] px-4 py-4">
      <div class="flex items-center justify-between mb-4">
        <a href="/" class="text-slate-700">
          <svg viewBox="0 0 24 24" class="h-6 w-6" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M15 18l-6-6 6-6" />
          </svg>
        </a>
        <h1 class="text-xl font-black text-slate-900">Artistes</h1>
        <button class="text-slate-700">
          <svg viewBox="0 0 24 24" class="h-6 w-6" fill="currentColor">
            <path d="M3 6h18M3 12h18M3 18h18" />
          </svg>
        </button>
      </div>

      <form method="GET" class="mb-6">
        <div class="relative">
          <svg viewBox="0 0 24 24" class="absolute left-3 top-1/2 -translate-y-1/2 h-5 w-5 text-slate-600">
            <circle cx="11" cy="11" r="8" fill="none" stroke="currentColor" stroke-width="2" />
            <path d="m21 21-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
          </svg>
          <input
            type="text"
            name="search"
            value={searchQuery}
            placeholder="Rechercher un artiste..."
            class="w-full rounded-full bg-white pl-10 pr-4 py-2.5 text-sm text-slate-700 placeholder-slate-500 outline-none"
          />
        </div>
      </form>
    </section>

    <section class="px-4 py-6 space-y-4">
      {filteredArtistes.length === 0 ? (
        <div class="rounded-3xl bg-white p-8 text-center shadow-sm">
          <p class="text-lg text-slate-600">Aucun artiste ne correspond à votre recherche.</p>
        </div>
      ) : (
        filteredArtistes.map((artiste) => {
          const firstPassage = artiste.passages[0];
          return (
            <a href={`/artistes/${artiste.id}`} class="block">
              <div class="rounded-2xl bg-white p-4 shadow-sm hover:shadow-md transition">
                <div class="flex gap-4">
                  <!-- Image -->
                  <div class="relative shrink-0">
                    {getImageUrl(artiste) ? (
                      <img
                        src={getImageUrl(artiste)}
                        alt={artiste.nom}
                        class="h-20 w-20 object-cover rounded-lg"
                      />
                    ) : (
                      <div class="h-20 w-20 bg-linear-to-br from-slate-300 to-slate-400 rounded-lg" />
                    )}
                    <!-- Cœur favoris -->
                    <button class="favorite-btn absolute -top-2 -right-2 bg-blue-200 rounded-full p-1" data-id={artiste.id}>
                      <span class="heart-outline block">
                        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="white" stroke="currentColor" stroke-width="1.5">
                          <path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.6l-1-1a5.5 5.5 0 0 0-7.8 7.8l1 1L12 21l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8Z" />
                        </svg>
                      </span>
                      <span class="heart-filled hidden">
                        <svg viewBox="0 0 24 24" class="h-5 w-5" fill="#ef4444">
                          <path d="M12 21s-7.1-4.5-9.5-9.1A5.9 5.9 0 0 1 12 4.8a5.9 5.9 0 0 1 9.5 7.1C19.1 16.5 12 21 12 21Z" />
                        </svg>
                      </span>
                    </button>
                  </div>

                  <!-- Infos -->
                  <div class="flex-1 min-w-0">
                    <h2 class="text-base font-black text-[#4f9ef7]">
                      {artiste.nom}
                    </h2>

                    {firstPassage && (
                      <div class="mt-2 space-y-1">
                        <div class="flex items-center gap-2 text-sm text-slate-700">
                          <svg viewBox="0 0 24 24" class="h-4 w-4 fill-slate-700 shrink-0">
                            <rect x="3" y="4" width="18" height="18" rx="2" ry="2" fill="none" stroke="currentColor" stroke-width="2"/>
                            <line x1="16" y1="2" x2="16" y2="6" stroke="currentColor" stroke-width="2"/>
                            <line x1="8" y1="2" x2="8" y2="6" stroke="currentColor" stroke-width="2"/>
                          </svg>
                          {formatDatePassage(firstPassage.date)}
                        </div>
                        <div class="flex items-center gap-2 text-sm text-slate-700">
                          <svg viewBox="0 0 24 24" class="h-4 w-4 fill-slate-700 shrink-0">
                            <path d="M12 2a7 7 0 0 0-7 7c0 4.8 5.7 11.6 6 11.9a1.3 1.3 0 0 0 2 0c.3-.3 6-7.1 6-11.9a7 7 0 0 0-7-7Zm0 9.5A2.5 2.5 0 1 1 12 6a2.5 2.5 0 0 1 0 5.5Z" />
                          </svg>
                          {firstPassage.expand?.scene?.nom}
                        </div>
                      </div>
                    )}
                  </div>
                </div>
              </div>
            </a>
          );
        })
      )}
    </section>


  <script>
    const storageKey = 'festival-favoris-artistes';

    function getFavorites() {
      try {
        return JSON.parse(localStorage.getItem(storageKey) || '[]');
      } catch {
        return [];
      }
    }

    function saveFavorites(favorites: string[]): void {
      localStorage.setItem(storageKey, JSON.stringify(favorites));
    }

    function refreshFavorites() {
      const favorites = getFavorites();

      document.querySelectorAll('.favorite-btn').forEach((button: Element) => {
        const id = (button as HTMLElement).dataset.id;
        const isFavorite = favorites.includes(id);

        const outline = button.querySelector('.heart-outline');
        const filled = button.querySelector('.heart-filled');

        if (outline && filled) {
          outline.classList.toggle('hidden', isFavorite);
          filled.classList.toggle('hidden', !isFavorite);
        }
      });
    }

    refreshFavorites();

    document.querySelectorAll('.favorite-btn').forEach((button: Element) => {
      button.addEventListener('click', (e: Event) => {
        e.preventDefault();
        const id = (button as HTMLElement).dataset.id;
        const favorites = getFavorites();

        if (favorites.includes(id)) {
          saveFavorites(favorites.filter((fav: string) => fav !== id));
        } else {
          saveFavorites([...favorites, id]);
        }

        refreshFavorites();
      });
    });
  </script>
      <nav class="fixed bottom-0 left-0 right-0 flex justify-around bg-white border-t border-slate-200 px-4 py-3">
      <a href="/" class="flex flex-col items-center gap-1 text-xs font-semibold text-slate-600 hover:text-slate-900">
        Accueil
      </a>
      <a href="/artistes" class="flex flex-col items-center gap-1 text-xs font-semibold text-slate-600 hover:text-slate-900">
        Artistes
      </a>
      <a href="/infos-pratiques" class="flex flex-col items-center gap-1 text-xs font-semibold text-[#4f9ef7]">
        Infos Pratiques
      </a>
      <a href="/contact" class="flex flex-col items-center gap-1 text-xs font-semibold text-slate-600 hover:text-slate-900">
        Contact
      </a>
    </nav>
</Layout>